Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %>
<br>

Comments:<br>
<ul>
  <% @job.comments.each do |comment| %>
  <li>
    Created: <%= comment.created_at %>
    <%= comment.body %>
  </li>
  <% end %>
</ul>
<%= link_to "Add Comment", new_company_job_comment_path(@job.company, @job, @comment) %>
<br>

<%= link_to "Delete", company_job_path(@job.company, @job), method: :delete %>
